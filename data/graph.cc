#include "graph.h"
#include "header2.hh" //to demonstrate 2 header files from same source header with gm2aie_nb def
#include "otheradf.h" //to demonstarte other header file from other source header with other gm2aie_nb def
//#include <vector>

myGraph gradf2;
myGraph2 gradf;
int main(int argc, char* argv[]) {
	char a[156], b[256];
	gradf2.in[2].gm2aie_nb(a, 256);
	//gradf.in.gm2aie_nb(a, 256, 512);
	test2 t2;
	t2.tt.hello();
}
